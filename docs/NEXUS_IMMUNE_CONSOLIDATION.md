# NEXUS × IMMUNE — consolidation record (2026-09-05)

## Canon

- NEXUS dynamics source lives here (Lorenz / harmonic / vanderpol / duffing / lotka / nemo).
- The runtime lives inside IMMUNE — not a second product Space:
  - `POST /api/immune/nexus/run` (SENTRA admits compact YAWAR payloads: hashes, not floats)
  - `https://szlholdings-immune.hf.space/nexus.html` (analog runtime page)
- Source pin declared on this repo: `szl-holdings/nexus@617fb49`.
- Product path: `a-11-oy.com/nexus` · proof path: `a11oy.net/nexus/`.
- Energy: UNAVAILABLE. Λ = Conjecture 1 OPEN.

## Private personal Spaces

`betterwithage/NEXUS` and `betterwithage/Nexus Probe` stay PAUSED + PRIVATE.
Estate rule: pause+private, never delete. They are history, not front doors.

## Grok-chrome removal checklist (main carries these today)

- [ ] `src/routes/__root.tsx` — drop `/__grok/manifest.webmanifest` and `/__grok/icon-180.png` link tags
- [ ] `public/__grok/` assets
- [ ] `AGENTS.md` sandbox scaffolding (if present at root)
- [ ] `server/` Grok dev-server leftovers (if superseded by the IMMUNE runtime)

Estate doctrine (szl-estate-os docs): never dump Grok sandbox chrome onto GitHub;
nexus and szl-khipu are not new front doors.

## Branch inventory (13) — recommended disposition

Keep: `main`, `analog-circuits`, `analog-anatomy`, `analog-a4-f18`, `analog-nemo`, `f19-weighted`, `puriq-aggregators` (active dynamics lanes).
Prune after merge-or-abandon review: `codex/truthful-hf-deploy-20260831`, `fix/hf-boot-stdlib`, `fix/hf-central-delegation-source-binding-v1`, `fix/hf-token-alias-fallback`, `hub-nemo`, `ops/hf-publish-every-main-source-20260831`, `staging/hf-publish-every-main-20260831` (publish/fix lanes whose purpose is served or stale).
